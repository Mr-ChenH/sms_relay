services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    image: sms-relay:local
    restart: unless-stopped
    environment:
      TZ: "${TZ:-Asia/Shanghai}"
    ports:
      - "8080:8080"
      - "1883:1883"
    volumes:
      - ./data:/data
